import { notFound } from "next/navigation";

/**
 * Demo Route Decommissioned
 * OmniService is now in production-ready mode. Demo sandboxes are fully removed.
 */
export default function DemoPage() {
  notFound();
}
